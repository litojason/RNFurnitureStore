const logo = {
  // logo: require('../assets/logo/logo.png'),
  logoHorizontal: require('../assets/logo/logo-horizontal.png'),
};

const image = {
  landing: require('../assets/landing.png'),
};

const assets = {logo, image};

export default assets;
